# GraphQL Error

## Issue 87: refactor: Migrate chatbot resolvers to shared GraphQL error handling
DRY up error logic across chatbot queries and mutations by leveraging shared error handler infrastructure. Migrate away from custom/inline try/catch blocks and GraphQLError calls in chatbot resolvers.

**Implementation Steps**
- Audit chatbot resolvers for custom error logic
- Refactor to use shared error handler/wrappers (withChatbotErrorHandling or generic as appropriate)
- Remove inline error code/extensions logic
- Update associated tests for new standardized error extensions
- Remove now-unused error constants

**Acceptance Criteria**
- Chatbot errors handled only via shared infrastructure, no manual error creation
- Resolvers/tests produce only the new error shape/extensions, and pass

## Issue 86: refactor: Migrate article resolvers to shared GraphQL error handling
Refactor all article query/mutation resolvers to use a shared error handler. Remove duplicated try/catch and GraphQLError calls in article resolver code; use shared error factories, codes, and wrapper.

**Implementation Steps**
- Identify custom try/catch and manual error code blocks in article resolvers
- Apply shared error handler (withArticleErrorHandling or generic if possible)
- Remove manual GraphQLError logic, use error factories/wrappers
- Rewrite affected tests to match standardized error extensions
- Remove article error constants if not needed

**Acceptance Criteria**
- Article resolvers use centralized error infra, no custom error code scattered
- Errors produce only new formatted extensions, tests green

## Issue 85: refactor: Migrate project resolvers to shared GraphQL error handling
Refactor all project resolver logic to use the shared error handling infrastructure (createErrorHandler, Errors, ErrorCodes). Remove any manual try/catch and error instance handling in project mutations and queries.

**Implementation Steps**
- Search for custom try/catch/manual error code extensions in project resolvers
- Apply createErrorHandler or new withProjectErrorHandling for all project operations
- Replace inline GraphQLError with error factories/wrappers
- Update tests, ensure errors produce new standard format/extensions
- Remove project-specific error consts if now redundant

**Acceptance Criteria**
- Project errors surfaced via shared infra only, with correct extensions/codes
- All tests pass, errors correctly mapped and tested

## Issue 84: refactor: Migrate user resolvers to shared GraphQL error handling
Adopt the centralized GraphQL error handling (createErrorHandler, Errors, ErrorCodes) in all user-related resolvers. Replace duplicated try/catch blocks and inline error code logic in user mutation/query resolvers with shared error factories/wrappers.

**Implementation Steps**
- Identify all custom error handling in user resolvers (register, login, mutations, queries)
- Refactor to use error factories and `withUserErrorHandling` as needed
- Remove inline GraphQLError creations and replace with error wrapper
- Refactor/validate tests to expect standard error structure and extensions
- Delete user-specific error constants if now redundant

**Acceptance Criteria**
- All user resolver errors return standard extensions (code, etc.), no manual GraphQLError logic remains
- Tests green, errors correctly handled in consumers