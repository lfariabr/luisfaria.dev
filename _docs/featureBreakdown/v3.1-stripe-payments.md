# v3.1 — Stripe Payments (Coffee + Meeting)

## GitHub Epic + Sub-Issues

- Epic: [#146](https://github.com/lfariabr/luisfaria.dev/issues/146)
- Backend config/service: [#147](https://github.com/lfariabr/luisfaria.dev/issues/147)
- Backend GraphQL mutation: [#148](https://github.com/lfariabr/luisfaria.dev/issues/148)
- Frontend mutation/hook: [#149](https://github.com/lfariabr/luisfaria.dev/issues/149)
- APOD-style FAB + dialog: [#150](https://github.com/lfariabr/luisfaria.dev/issues/150)
- Success/cancel pages: [#151](https://github.com/lfariabr/luisfaria.dev/issues/151)
- Analytics events: [#152](https://github.com/lfariabr/luisfaria.dev/issues/152)
- Tests + QA: [#153](https://github.com/lfariabr/luisfaria.dev/issues/153)
- Docs/env: [#154](https://github.com/lfariabr/luisfaria.dev/issues/154)

---

## Goal

Add a public Stripe checkout flow so visitors can:
- Buy me a coffee (AUD 5)
- Book a meeting (AUD 150)

UX must mirror the APOD pattern:
1. Global floating CTA button
2. Transparent selection dialog
3. Redirect to Stripe Checkout
4. Return to success/cancel pages

---

## Architecture

### Checkout path

`StripeFab` → `StripeDialog` → GraphQL mutation `createCheckoutSession(input)` → backend Stripe service → Stripe hosted checkout URL → redirect.

### Product model

Hardcoded keys (server-side validated):
- `coffee`
- `meeting`

Price IDs are injected from env:
- `STRIPE_COFFEE_PRICE_ID`
- `STRIPE_MEETING_PRICE_ID`

### Security model

- Stripe secret key is backend-only (`STRIPE_SECRET_KEY`)
- Mutation is intentionally public (support flow does not require auth)
- Server controls valid `productKey` and checkout session creation

---

## Implementation Summary

### Backend

- Added Stripe dependency in `backend/package.json`
- Added Stripe-related config fields in `backend/src/config/config.ts`
- Added Stripe service:
  - `backend/src/services/stripe.ts`
  - creates checkout sessions with success/cancel URLs
  - includes test-mode safe stub behavior
- Added GraphQL types:
  - `backend/src/schemas/types/stripeTypes.ts`
- Added GraphQL resolver:
  - `backend/src/resolvers/stripe/mutations.ts`
  - validates `productKey` (`coffee | meeting`)
  - maps failures to controlled GraphQL internal error
- Wired schema/resolvers:
  - `backend/src/schemas/typeDefs.ts`
  - `backend/src/resolvers/index.ts`
- Permission update:
  - `backend/src/validation/shield.ts`
  - `createCheckoutSession: allow`

### Frontend

- Added mutation + types:
  - `frontend/src/lib/graphql/mutations/stripe.mutations.ts`
  - `frontend/src/lib/graphql/types/stripe.types.ts`
- Added checkout hook:
  - `frontend/src/lib/hooks/useStripeCheckout.ts`
  - fires analytics and redirects via `window.location.assign`
- Added UI:
  - `frontend/src/components/stripe/StripeFab.tsx`
  - `frontend/src/components/stripe/StripeDialog.tsx`
- Mounted globally in root layout:
  - `frontend/src/app/layout.tsx`
  - Stripe FAB stacks above APOD FAB
- Added payment return pages:
  - `frontend/src/app/payment/success/page.tsx`
  - `frontend/src/app/payment/cancel/page.tsx`

### Documentation

- Added Stripe env variables to project README:
  - `FRONTEND_URL`
  - `STRIPE_SECRET_KEY`
  - `STRIPE_COFFEE_PRICE_ID`
  - `STRIPE_MEETING_PRICE_ID`
  - `STRIPE_WEBHOOK_SECRET` (phase-2 reserved)

---

## Analytics Events

- `stripe_fab_opened`
- `stripe_item_selected`
- `stripe_checkout_started`
- `stripe_checkout_error`

---

## Validation & Tests

### Automated

- Backend unit test:
  - `backend/src/__tests__/unit/stripe.mutations.test.ts`
  - covers valid flow, invalid product key, and service-error mapping
- Frontend component test:
  - `frontend/src/__tests__/components/Stripe.test.tsx`
  - covers FAB rendering, dialog flow, and checkout trigger

### Build checks

- `backend`: build passes (`npm run build`)
- `frontend`: targeted Stripe tests pass; full `next build` has intermittent environment hang during optimization (non-Stripe-specific)

---

## Local Development Setup

> **Goal:** a new engineer should be able to run the full checkout flow locally using only this guide.

### 1. Get Stripe test keys

1. Log in to [dashboard.stripe.com](https://dashboard.stripe.com) and switch to **Test mode** (toggle top-left).
2. Go to **Developers → API keys**.
3. Copy the **Secret key** (`sk_test_...`) → `STRIPE_SECRET_KEY` in `backend/.env`.

### 2. Create test products

Go to **Products** (still in Test mode) and create two one-time products:

| Product | Price | Variable |
|---------|-------|----------|
| Coffee | AUD 5 | `STRIPE_COFFEE_PRICE_ID` |
| Meeting | AUD 150 | `STRIPE_MEETING_PRICE_ID` |

After saving each product, copy the **Price ID** (`price_...`) into `backend/.env`.

### 3. Configure `FRONTEND_URL`

```bash
# backend/.env
FRONTEND_URL=http://localhost:3000   # no trailing slash, no path suffix
```

This value is used to build `cancel_url` and `success_url` for Stripe sessions. A trailing slash or extra path will cause redirect failures in local and production.

### 4. Start the stack

```bash
cd backend && npm run dev   # :4000
cd frontend && npm run dev  # :3000
```

Click the floating **💳** button on the site → pick Coffee or Meeting → you'll be redirected to a Stripe-hosted test checkout. Use card `4242 4242 4242 4242` with any future expiry and CVC.

### 5. `STRIPE_WEBHOOK_SECRET` — leave blank for now

Webhook fulfillment is Phase 2 (see below). Leave `STRIPE_WEBHOOK_SECRET` empty locally — the checkout redirect flow works without it.

---

## Current Limitations / Phase 2

- **[#152](https://github.com/lfariabr/luisfaria.dev/issues/152) — Analytics events** *(upcoming)*: `stripe_fab_opened`, `stripe_item_selected`, `stripe_checkout_started`, `stripe_checkout_error` are wired into the hook but the analytics provider integration needs validation across all environments.
- **Webhook fulfillment** — `STRIPE_WEBHOOK_SECRET` is reserved in config. Phase 2 will add a `/api/stripe/webhook` endpoint to persist payment results and trigger confirmation emails via Resend. Deferred because it requires a publicly reachable URL (ngrok or production) and a more complex test harness.
- **No payment persistence** — successful sessions are not stored in MongoDB yet (blocked by webhooks).
- **No admin dashboard** — support transaction reporting is deferred to a later milestone.

---

## Release Notes Draft

`v3.1.0` introduces Stripe support payments with two fixed purchase options (coffee/meeting), APOD-style floating CTA, hosted checkout redirect, and success/cancel return pages. Webhook fulfillment and analytics event validation are tracked as follow-on issues (#152, phase 2).
